import React from 'react';const BasketItem = (props) => {  const {    id,    name,    price,    quantity,    removeFromBasket = Function.prototype,    incrementCount = Function.prototype,    decQuantity = Function.prototype,  } = props;  return (    <li className="collection-item">{name} <i onClick={() => decQuantity(id)} className={'material-icons basket-quantity'}>remove</i> x{quantity} <i onClick={() => incrementCount(id)} className={'material-icons basket-quantity'}>add</i> = {price}      <span className="secondary-content" onClick={() => removeFromBasket(id)}><i className="material-icons basket-delete" >close</i></span>    </li>  );};export default BasketItem;